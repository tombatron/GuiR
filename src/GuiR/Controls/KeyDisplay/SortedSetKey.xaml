<UserControl x:Class="GuiR.Controls.KeyDisplay.SortedSetKey"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuiR.Controls.KeyDisplay"
             xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Behaviors:Interaction.Triggers>
        <Behaviors:EventTrigger EventName="Loaded">
            <Behaviors:InvokeCommandAction Command="{Binding LoadKeyValue}" />
        </Behaviors:EventTrigger>
    </Behaviors:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <CheckBox Margin="10, 10" IsChecked="{Binding IsGeoValue, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0">Is Geo data?</CheckBox>

        <ScrollViewer Margin="10,10,10,10" BorderBrush="Black" BorderThickness="5" Grid.Row="1">
            <DataGrid x:Name="SortedSetItems" ItemsSource="{Binding KeyValue, UpdateSourceTrigger=PropertyChanged}" />
        </ScrollViewer>            
    </Grid>
</UserControl>
